<h1>New Post</h1>

<% if @post.errors.any? %>

    <% @post.errors.full_messages.each do |err| %>
        <p><%= err %></p>
    <% end %>

<% end %>

<%= form_with model: @post do |f| %>

    <%= f.text_field :body %>

    <%= f.submit "Post" %>

<% end %>